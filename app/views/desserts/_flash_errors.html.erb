<% if @dessert.errors.any? %>
   <div id="error_explanation">
     <h2>
       <%= pluralize(@dessert.errors.count, "error") %> prohibited
       this article from being saved:
     </h2>
     <ul>
       <% @dessert.errors.full_messages.each do |msg| %>
         <li><%= msg %></li>
       <% end %>
     </ul>
   </div>
 <% end %>
